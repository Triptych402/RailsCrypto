<%= form_with(model: bitcoin, local: true) do |form| %>
  <% if bitcoin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bitcoin.errors.count, "error") %> prohibited this bitcoin from being saved:</h2>

      <ul>
      <% bitcoin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :symbol %>
    <%= form.text_field :symbol, id: :bitcoin_symbol %>
  </div>

  <div class="field">
    
    <%= form.number_field :user_id, value: current_user.id, type: "hidden" %>
  </div>

  <div class="field">
    <%= form.label :cost_per %>
    <%= form.text_field :cost_per, id: :bitcoin_cost_per %>
  </div>

  <div class="field">
    <%= form.label :amount_owned %>
    <%= form.text_field :amount_owned, id: :bitcoin_amount_owned %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
